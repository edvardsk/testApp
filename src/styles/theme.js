import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2372e8',
    },
    secondary: {
      main: '#6a9f19',
    },
    background: {
      default: '#f8f9fa',
    },
  },
});

export default theme;
